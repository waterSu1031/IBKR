from ib_insync import MarketOrder

def create_order(action: str, quantity: int):
    return MarketOrder(action, quantity)
from ib_insync import MarketOrder

def create_order(action: str, quantity: int):
    return MarketOrder(action, quantity)
